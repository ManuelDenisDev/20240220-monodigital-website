---
import Logo from '@shared/Logo.astro'
import ButtonDarkMode from '@shared/Buttons/ButtonDarkMode.astro'
import HeaderNav from '@shared/Header/HeaderNav.tsx'
import HeaderNavMv from '@shared/Header/HeaderNavMv.tsx'
import ButtonAcentuadoXs from '@shared/Buttons/ButtonAcentuadoXs.astro'
---

<header class="monoHeader">
    <div class="monoHeader__wrapper">
        <div class="monoHeader__container shadow-massive">
            <div class="flex items-center justify-between">
                <Logo estilo="w-20" />
            </div>
            <div class="flex gap-4 items-center list-none">
                <HeaderNavMv client:load />
                <HeaderNav client:load />
                <div class="hidden md:flex">

                    <ButtonAcentuadoXs url="/contacto" texto="Contacto"/>
                </div>
                <ButtonDarkMode />
            </div>
        </div>
    </div>
</header>

<style>
    .monoHeader {
        @apply fixed inset-x-0 top-8 md:top-8 z-50;
    }
    .monoHeader__wrapper {
        @apply mx-auto px-8 md:max-w-3xl;
    }
    .monoHeader__container {
        @apply flex items-center justify-between backdrop-blur-xl backdrop-filter bg-onyx-900/60 dark:bg-onyx-200/60 border border-onyx-800 dark:border-onyx-300  mx-auto p-2 relative lg:rounded-2xl rounded-xl w-full;
    }
</style>